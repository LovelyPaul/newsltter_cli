# 핵심 기능 라이브러리
feedparser>=6.0.0
beautifulsoup4>=4.9.0
requests>=2.25.0
pyyaml>=5.4.0
jinja2>=3.0.0
python-dateutil>=2.8.0

# 환경 변수 및 설정
python-dotenv>=0.19.0

# AI 요약 라이브러리 (선택사항 - 필요한 것만 설치)
google-generativeai>=0.3.0
openai>=1.0.0
anthropic>=0.7.0

# 공휴일 체크 (선택사항)
workalendar>=16.0.0

# 테스트 (개발용)
pytest>=7.0.0